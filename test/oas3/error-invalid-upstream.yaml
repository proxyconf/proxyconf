x-proxyconf:
  security:
    auth:
      downstream: disabled

servers:
  # invalid upstream url
  - url: "/"
paths:
  /test:
    get:
      responses:
        '403':
          description: "Error"
        
