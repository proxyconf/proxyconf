x-proxyconf:
  url: https://localhost:44444/tls
  listener:
    address: "127.0.0.1"
    port: 44444
  security:
    auth:
      downstream:
        type: mtls
        trusted-ca: /tmp/proxyconf/ca-cert.pem
        clients:
          test_client:
            - exunit-good

servers:
  - url: http://127.0.0.1
paths:
  /test:
    get:
      responses:
        '200':
          description: "OK"
          content:
            application/json:
              example: '{"hello":"world"}'
              schema:
                type: object
