x-proxyconf-api-url: http://localhost:8080/error-missing-downstream-auth
x-proxyconf-cluster-id: proxyconf-exunit
x-proxyconf-id: error-missing-downstream-auth
x-proxyconf-listener:
  address: 127.0.0.1
  port: 8080
servers:
  - url: http://127.0.0.1:8087
openapi: 3.1.0
paths:
  /test:
    get:
      responses:
        # 403 is returned if the shared listener has been setup by previous tests
        '403': {
          # text/plain is the default content type used by envoy 404
          'text/plain': {}
        }
